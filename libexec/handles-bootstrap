#!/bin/bash
# Usage: handles bootstrap
# Summary: Resolve all dependencies that the project requires to run

set -e

cd "$(git rev-parse --show-toplevel 2>/dev/null)"
test -f "bin/bootstrap" && exec bin/bootstrap "$@"

if [ -f ".gitmodules" ]; then
  echo "==> Cloning into submodules…"
  git submodule update --init --recursive
fi

if [ "$(uname -s)" = "Darwin" ]; then
  if [ -f "Brewfile" ] && ! brew bundle check &>/dev/null; then
    echo "==> Installing Homebrew dependencies…"
    brew bundle
  fi

  if [ -f ".node-version" ]; then
    command -v nodenv || echo "!!! Error: nodenv is not installed" >&2 && exit 1

    if ! nodenv version-name &>/dev/null; then
      echo "==> Installing Node…"
      nodenv install --skip-existing
    fi
  fi

  if [ -f ".python-version" ]; then
    command -v pyenv || echo "!!! Error: pyenv is not installed" >&2 && exit 1

    if ! pyenv version-name &>/dev/null; then
      echo "==> Installing Python…"
      pyenv install --skip-existing

      if ! pyenv which pipenv >/dev/null 2>&1; then
        pip install pipenv
        pyenv rehash
      fi
    fi
  fi

  if [ -f ".ruby-version" ]; then
    command -v rbenv || echo "!!! Error: rbenv is not installed" >&2 && exit 1

    if ! rbenv version-name &>/dev/null; then
      echo "==> Installing Ruby…"
      rbenv install --skip-existing

      if ! rbenv which bundle >/dev/null 2>&1; then
        gem install bundler
        rbenv rehash
      fi
    fi
  fi
fi

if [ -f "Gemfile" ]; then
  echo "==> Installing gem dependencies…"
  bundle check &>/dev/null || bundle install --without production
fi

if [ -f "glide.yaml" ]; then
  command -v glide || echo "!!! Error: glide is not installed" >&2 && exit 1

  echo "==> Installing go dependencies…"
  glide install --strip-vendor
fi

if [ -f "package.json" ]; then
  echo "==> Installing npm dependencies…"

  if [ -f "yarn.lock" ]; then
    command -v yarn || echo "!!! Error: yarn is not installed" >&2 && exit 1

    yarn install
  else
    npm install
  fi
fi

if [ -f "Pipfile" ]; then
  if pipenv --venv &>/dev/null; then
    PYTHON_REQUESTED="$(pyenv prefix)/bin/python"

    if [ "$(pipenv run python --version 2>&1)" != "$("$PYTHON_REQUESTED" --version 2>&1)" ]; then
      PYTHON_OUTDATED=1
    fi
  fi

  echo "==> Installing pip dependencies…"
  pipenv install --dev ${PYTHON_OUTDATED:+--python="$PYTHON_REQUESTED"}
fi
